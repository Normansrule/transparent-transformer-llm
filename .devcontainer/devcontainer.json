{
  "name": "transparent-transformer-llm classroom",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "pip install -r requirements.txt jupyter && echo 'Ready. Try:  python -m transparent_transformer.trace   |   make tour   |   python classroom/check.py' ",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter", "bierner.markdown-mermaid"],
      "settings": { "workbench.startupEditor": "readme" }
    },
    "codespaces": { "openFiles": ["START_HERE.md"] }
  }
}
